<!-- comentarios.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comentários dos Jogos</title>
</head>
<body>
    <h1>Comentários dos Jogos</h1>

    <% if (array.length === 0) { %>
        <p>Nenhum jogo com comentários encontrado.</p>
    <% } else { %>
        <ul>
            <% array.forEach(jogo => { %>
                <li>
                    <strong>Nome do Jogo:</strong> <%= jogo.name %> <!-- Substitua "nome" pelo campo correto em seu esquema de dados -->
                    <br>
                    <strong>ID do Jogo:</strong> <%= jogo._id %>
                    <br>
                    <strong>Comentários:</strong>
                    <ul>
                        <% jogo.comments.forEach(comentario => { %>
                            <li><%= comentario.commit %></li>
                        <% }); %>
                    </ul>
                </li>
            <% }); %>
        </ul>
    <% } %>
</body>
</html>
